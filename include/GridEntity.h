#pragma once
//#include "field.h"

class GridEntity{   //абстрактный класс-интерфейс, отвечающий за будущее добавление сущностей в игру.
/*protected:
    bool alive;
    int x;
    int y;
    Field *currField;
public:
    GridEntity(int x, int y, Field *field);
    int getX();
    int getY();
    bool isAlive();
    enum EntityType{CREATURE, ITEM};
    EntityType getType();
    EntityType entType;
    virtual ~GridEntity() = default;*/
}; 
